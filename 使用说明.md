# 微信图片轻压缩 v1.0

双击 `index.html` 即可在桌面浏览器中使用。拖入或选择 JPG、PNG、WebP 图片后会自动处理；粘贴图片先进入待压缩队列，点击“开始压缩”后统一处理。多张结果可以打包下载 ZIP。

粘贴图片时，先点击“从剪贴板粘贴图片”输入框，再按 **Ctrl + V**；可以连续粘贴多张。图片会先进入输入框下方的待压缩队列，可逐张移除。确认后点击旁边的“开始压缩”，才会统一生成结果。输入框会显示选中、入队、处理完成或剪贴板无可用图片的状态。直接在页面按 Ctrl + V 也会加入同一队列。

图片只在浏览器当前页面处理，没有账号、服务器、外部脚本或网络请求。关闭页面后，结果会从页面内存中移除。

文件上限按十进制计算：**80KB = 80,000 字节**。已低于该上限的原图直接保留原格式；其余图片输出为 JPEG。透明区域会合成为白色背景。个别极长或信息密集的图片可能需要降到 960 像素以下，文字清晰度仍建议在实际微信查看场景中检查。

当页面判断 80KB 结果的细节可能受损时，会显示“尝试更清晰版本”。点击后会生成一个不超过 **150KB = 150,000 字节** 的版本，优先利用 100–150KB 的空间；若原图本来小于 150KB，则直接保留原图，不会为了凑满 100KB 而增加体积。两个版本都可单独下载，批量 ZIP 仍只包含默认 80KB 版本。

**画质选择方式：**两档使用同一套规则。程序先在少量宽度档位上搜索文件大小允许的最高 JPEG 质量，再比较整图结构、边缘保留情况，以及文字或线条密集区域的原尺寸局部细节。只有大文件版本在这些指标上有可辨别的提升，才会提供下载。页面的“放大对比”可以查看原图与输出图，生成大文件版本后也可一起比较。

桌面端将放入图片区与压缩结果并排显示。结果卡片里，“下载 80KB 版”获取默认结果，“放大对比”打开原尺寸对比窗口；若出现画质风险提示，可再点“生成清晰版 · ≤150KB”。多张图片的结果列表在右侧独立滚动。

这些指标只能估计视觉损失，不能像人一样读懂文字或判断数字是否正确。特别重要的财经数据图、小字号说明和表格，仍应在微信实际查看尺寸下人工确认。

## 分享给其他人

**最省事的离线方式：**直接发送 `WeChatImageCompressor_portable.html`。对方保存后双击打开即可使用，不需要同时携带 CSS、JavaScript 或其他文件。单文件版与此处的分文件版功能相同。

小范围试用：发送 `微信图片轻压缩_v1.0.zip`，对方解压后双击 `index.html` 即可使用。四个文件需放在同一文件夹。

长期分享：将 `index.html`、`style.css` 和 `app.js` 发布为静态 HTTPS 网页，直接发送链接。发布的是页面文件；用户选择或粘贴的图片仍只在其浏览器里处理。正式分享前，请用目标用户常用的浏览器和网络试用一次。

页面会分析缩略图以粗分照片、设计图和文字密集图，再搜索多种宽度和 JPEG 编码质量，依据结构相似度、边缘保留度和分辨率评分选取不超过 80KB 的结果。浏览器原生 JPEG 编码器无法固定控制色度抽样或逐行编码。
